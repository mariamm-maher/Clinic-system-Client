<div className="relative">
  {/* <div className="grid grid-cols-2 gap-3">
              {services.map((service, index) => (
                <ServiceCard
                  key={index}
                  icon={service.icon}
                  title={service.title}
                  description={service.description}
                  colorClass={service.colorClass}
                  rotateClass={service.rotateClass}
                />
              ))}
            </div> */}
  {/* <Card className="absolute -bottom-4 left-1/2 transform -translate-x-1/2 shadow-2xl">
              <CardContent className="p-4 text-center">
                <Avatar className="w-12 h-12 mx-auto mb-2">
                  <AvatarFallback className="bg-blue-600 text-white text-sm font-bold">
                    {clinicConfig.doctor.name
                      .split(" ")
                      .map((n) => n[0])
                      .join("")}
                  </AvatarFallback>
                </Avatar>
                <CardTitle className="text-sm mb-1">
                  {clinicConfig.doctor.name}
                </CardTitle>
                <CardDescription className="text-xs mb-2">
                  {clinicConfig.doctor.title}
                </CardDescription>{" "}                <Badge className="bg-blue-600 text-white text-xs hover:bg-blue-700 hover:scale-105 transition-all duration-300 cursor-default">
                  15+ Years Experience
                </Badge>
              </CardContent>
            </Card> */}
</div>;
